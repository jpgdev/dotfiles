[core]
align = top
color = blue
display_modules = distro(), uname(n), uname(r), uptime(), wm(), de(), packages(), ram(), cpu(), env(editor), fs(/), fs(/var), fs(/home), fs(/mnt/data),fs(/mnt/jeux),mpd(albums)
